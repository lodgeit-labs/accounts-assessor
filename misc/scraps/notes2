            /*foreach (Excel.Worksheet sheet in AddIn.Application.Worksheets)
                if (sheet.Name.ToLower().StartsWith(HPTemplate.sheetPrefix.ToLower()))
                */
http://xbrl.squarespace.com/journal/2019/12/19/modern-approach-to-creating-a-financial-reporting-scheme.html

IGraph g = new Graph {BaseUri = UriFactory.Create("https://lodgeit.net.au") };

                if (bStandalone)
                    MessageBox.Show("depreciation_query worksheet not found.", "Lodge iT");
                return false;
                
            DateTime date = GetCellValueAsDate(sheet, ((char)col).ToString() + row.ToString());
            ILiteralNode date_node = g.CreateLiteralNode(date.ToString("yyyy-MM-dd"), UriFactory.Create(XmlSpecsHelper.XmlSchemaDataTypeDate));
                

