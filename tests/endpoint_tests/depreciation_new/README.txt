basic single-asset query for both diminishing_value and prime_cost works, with pre-2006 purchase date too, with various date ranges

pools: we never resolved the uncertainty about pooling, so, that functionality is hidden by defaut.

here we have 3 testcases that are yet to be adapted to the automated runner, but, since we didn't touch the calculation code in, like, forever, then if it calculates, it's probably still the same correct result.

